# Environment variables for StudyBot Docker deployment

# Required: OpenAI API Key for embeddings and chat (if using OpenAI provider)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Anthropic API Key (only required if using Anthropic as AI provider)
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Optional: Secret key base for Phoenix (will be generated if not provided)
# SECRET_KEY_BASE=your-64-character-secret-key-here

# Optional: Host for the Phoenix application (default: localhost)
# PHX_HOST=localhost

# Optional: Port for the Phoenix application (default: 4000)
# PORT=4000